---
// Få adgang til `variant`, `size`, og `href` fra props
const { variant = "primary", size = "medium", href } = Astro.props;

// Definér knap-styling
const buttonStyling = "py-5 px-6 rounded-lg border-2 md transition ease-in-out uppercase";
const variantClasses = {
  primary: "text-white  bg-cerice_ text-poppins border-cerice_ hover:bg-white hover:text-cerice_",
  secondary: "text-white font-bold text-rubik border-graphite_light bg-graphite_light hover:bg-white hover:text-graphite_light",
  outlined: "text-[var(--greendark)] border-[var(--orange)] hover:bg-[var(--orange)] hover:text-[var(--greendark)]",
};
const sizeClasses = {
  large: "text-4xl",
  medium: "text-2xl",
  small: "text-xs",
};
---

<div>
  {
    href ? (
      <a href={href} class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}>
        <slot />
      </a>
    ) : (
      <button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}>
        <slot />
      </button>
    )
  }
</div>

<!-- ---
const { variant = "primary", size = "medium" } = Astro.props;
const buttonStyling = "text-[var(--offwhite)] py-5 px-6 rounded-lg border-2 border-[var(--orange)] rounded md transition ease-in-out uppercase";
const variantClasses = {
    "primary": "hover:bg-[var(--orange)]",
    "secondary": ""
};

const sizeClasses = {
    "large": "text-4xl",
    "medium":"text-2xl",
    "small":"text-xs"
};
---
<div>
    <button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>
</div> -->
